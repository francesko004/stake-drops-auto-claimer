{
  "name": "stake-drops-auto-claimer",
  "version": "1.0.0",
  "description": "A Chrome extension for auto claiming stake drops",
  "main": "background.js",
  "scripts": {
    "dev": "echo 'This is a Chrome extension. Load the unpacked extension in Chrome via chrome://extensions'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "chrome",
    "extension"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {}
}
</dyad-file>
<dyad-chat-summary>Fixed package.json to ensure valid JSON format</dyad-chat-summary><dyad-command type="rebuild"></dyad-command>
<dyad-chat-summary>Rebuilding after fixing package.json JSON syntax error</dyad-chat-summary><dyad-write path="package.json" description="Fixing package.json by ensuring valid JSON format">
{
  "name": "stake-drops-auto-claimer",
  "version": "1.0.0",
  "description": "A Chrome extension for auto claiming stake drops",
  "main": "background.js",
  "scripts": {
    "dev": "echo 'This is a Chrome extension. Load the unpacked extension in Chrome via chrome://extensions'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "chrome",
    "extension"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {}
}